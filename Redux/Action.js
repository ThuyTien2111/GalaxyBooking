import { createAction } from '@reduxjs/toolkit';

export const setTicketAdded=createAction('SET_TICKET_ADDED')
export const setHistory=createAction('SET_HISTORY')
export const setUser=createAction('SET_USER')

